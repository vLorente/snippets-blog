export async function getImage(src: string, basePath?: string) {
	// const files = import.meta.glob<ImageMetadata>("../../**", {
	// 	import: "default",
	// })
	// let normalizedPath = path
	// 	.normalize(path.join("../../", basePath, src))
	// 	.replace(/\\/g, "/")
	// const img = await files[normalizedPath]()
	// return img
}
